---
description: RagChunk Java / Spring Boot 生产代码、测试与端口适配规范
globs: "**/*.java"
alwaysApply: false
---

## Java 与依赖注入

- 使用 Java 17；类 `PascalCase`，方法/变量 `camelCase`，常量 `UPPER_SNAKE_CASE`，包名全小写。
- 生产代码使用构造器注入，依赖声明为 `final`；可用 `@RequiredArgsConstructor`，不使用字段 `@Autowired`。
- Spring 集成测试允许字段注入，以减少无意义搭建；纯单元测试直接构造对象。
- 日志使用 SLF4J/`@Slf4j`，不使用 `System.out`；异常保留可诊断上下文但不得泄露密钥和完整正文。
- 优先使用不可变值对象和 Java `record`；仅对确实需要可变绑定/持久化的 DTO、VO、Entity 使用 Lombok `@Data`。
- 不为简单对象机械添加 `@Builder`，不使用 `@AllArgsConstructor` 代替明确的依赖注入设计。

## RagChunk 分层

```text
controller
  → service/<domain> 或 ingest
  → Port 接口
  → MyBatis/Jdbc/InMemory/Local/HTTP Adapter
```

| 区域 | 负责 | 禁止 |
|---|---|---|
| `controller` | 路由、协议校验、状态码、VO | 业务规则、事务、直调 Mapper/Entity/具体 Adapter |
| `service/<domain>` | 用例编排、领域规则、跨 Port 协作 | Servlet 类型扩散、跨域直调 Mapper |
| `ingest` | 解析、规范化、切片、质量评估与入库流水线 | HTTP 控制器逻辑、具体数据库 SQL |
| Port | 描述可替换的存储或外部能力 | Spring MVC、SQL、供应商协议细节 |
| Adapter | 实现 Port，处理 MyBatis/Jdbc/文件/HTTP | 跨领域业务决策 |
| `mapper`/`entity` | PostgreSQL 映射 | 作为 API 输入输出、承载领域编排 |
| `dto`/`vo` | 领域数据和外部契约 | 密钥、连接对象、懒加载 Entity |

- 不要求每个 Service 都有接口；只有存在真实替换、测试隔离或稳定边界时才创建接口。
- 新的存储实现通过现有 Port 与 `@ConditionalOnProperty` 接入，避免业务层判断具体存储类型。
- 领域间通过 Service 或 Port 交互，不通过共享 Mapper 绕过边界。

## API、校验与异常

- REST 使用资源名词、正确 HTTP 动词和状态码；规模不可控的外部列表必须分页或显式设置上限。
- Controller 请求使用 Bean Validation 或等价的集中校验；业务校验放在对应 Service/Validator。
- 统一通过 `@RestControllerAdvice`/`@ExceptionHandler` 返回稳定错误结构。
- Entity 不直接作为 API 响应；请求 DTO 与响应 VO 只暴露契约需要的字段。
- OpenAPI 描述公共契约、错误码和关键示例；Javadoc 解释非显然约束，不复述代码。

## 事务与持久化调用

- 事务边界放在需要原子性的用例或持久化 Adapter 方法上，与类名是否包含 `Impl` 无关。
- 只读数据库操作可使用 `readOnly=true`；外部 HTTP、LLM、Embedding 和文件 IO 不放在长事务中。
- MyBatis-Plus 优先类型安全的 `LambdaQueryWrapper`；SQL 参数使用 `#{}`，不得用 `${}` 拼接用户输入。
- 面向外部、数据量不可控的查询必须分页或有界；内部已知有界集合可直接返回。
- 避免数据库 N+1 和无界循环写；有批量接口时优先批量，逐项外部调用应设置数量上限、超时和失败策略。
- 并发更新计数或状态时使用原子 SQL、乐观锁或显式锁策略，不能依赖无保护的读改写。

## 测试规则

- 新行为先写失败测试并确认有效 RED，再写最小实现。
- 纯逻辑和领域行为使用 `@Tag("unit")`；Spring 上下文、数据库、文件系统和网络边界使用 `@Tag("integration")`。
- 测试名称描述可观察行为；不通过反射锁定私有实现。
- mock 只放在真实外部边界；领域内部优先使用真实对象或轻量内存 Adapter。
- 不得用 `@Disabled`、删除断言、扩大容差或增加盲目重试来隐藏失败。
- 命令和证据格式遵循 `AGENTS.md` 与 `docs/TDD工作流.md`。
