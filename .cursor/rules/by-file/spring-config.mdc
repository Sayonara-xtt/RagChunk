---
description: RagChunk Spring Profile、存储模式、模型连接与敏感配置规则
globs: "src/main/resources/application*.{yml,yaml,properties}"
alwaysApply: false
---

## Profile 与覆盖关系

- `application.yaml` 只放跨环境默认值；环境差异放在 `application-{profile}.yaml` 或环境变量。
- 修改默认激活 Profile、数据库地址、模型地址或存储模式时，说明 local/docker/inmemory/array/ollama 的影响。
- 不复制整段配置；只覆盖环境真正不同的键，并保持键名与 `RagChunkProperties` 一致。
- 新增配置必须有明确默认值、用途、边界和测试；公共配置变化同步更新示例和文档。

## 存储与向量不变量

- `ragchunk.storage.mode=postgres` 时必须存在匹配的元数据 Store 和 VectorStore；`inmemory` 时不得依赖 DataSource/MyBatis/Flyway。
- `ragchunk.storage.vector-store` 只使用项目已有的 `pgvector`、`array`、`inmemory`，新增实现需先扩展 Port/Adapter 并测试条件 Bean 唯一性。
- `ragchunk.embedding.dimensions` 必须与数据库向量列和模型实际输出一致，不能只修改 YAML。
- 知识库可快照模型名、切片和检索参数，但不得快照 API Key、密码或 Token。

## 密钥与连接配置

- 密钥、数据库密码和 Token 使用环境变量；提交文件中的默认值只能是无效开发占位值。
- 日志和运行时诊断接口最多暴露“是否已配置”，不得返回密钥内容。
- 外部 LLM/Embedding URL 必须包含连接/请求超时策略；本地地址不得作为生产假设。
- 新增供应商配置前先抽象外部边界，避免在领域 Service 中散布供应商判断。

## 修改后的验证

- 至少运行与目标 Profile 对应的上下文或配置绑定测试。
- PostgreSQL/pgvector 变化运行 Testcontainers 集成测试；内存模式变化运行 `RagChunkApplicationTests`。
- 不得为使上下文启动而静默排除必要的自动配置或放宽 Bean 条件。
