# RagChunk AI 开发规则

## 版本开发门禁

凡按版本路线实施的功能、缺陷修复、架构演化或治理改造，必须先在 `docs/versions/Vx.x/` 建立版本文档：

1. `01-需求单.md`：目标、范围、不包含项、验收标准、约束和风险等级；
2. `02-详细设计.md`：架构、模块、接口、数据、异常、安全、兼容、迁移和回滚设计；
3. `03-测试用例设计.md`：需求对应的单元、集成、契约和 E2E 用例，以及 Given / When / Then、Mock 边界和关键断言；
4. `04-实施与验收报告.md`：只在获准实施后回填实际变更、RED/GREEN、回归证据、偏差和剩余风险。

执行两道人工门禁：

- 门禁 A：先提交需求单，开发者明确批准后才能编写详细设计和测试用例设计；
- 门禁 B：详细设计和测试用例设计均经开发者明确批准后，才能新增测试代码或修改生产代码；
- AI 不得自行批准、从沉默推断批准，或把“继续分析”视为“批准实施”；
- 审核意见引起范围、契约、数据、安全或验收标准变化时，必须更新受影响文档并重新通过对应门禁；
- 每个版本验收结束后必须停止，等待开发者发出下一个版本指令。

版本风险等级决定文档深度，但不得省略三份前置文档：

| 等级 | 典型变化 | 最低要求 |
|---|---|---|
| L1 | 内部重构、小型缺陷、低风险配置 | 三份精简文档，说明无外部行为变化及回归范围 |
| L2 | 新业务能力、API、持久化或异步流程调整 | 三份完整文档，包含兼容、失败场景和集成验证 |
| L3 | 权限、安全、数据迁移、删除、并发、不可逆操作 | L2 全部内容，加威胁/数据风险、回滚或备份、人工验收清单 |

每份文档必须包含状态、版本、风险等级、审核人、审核时间和变更记录。状态只允许：`草稿`、`待审核`、`已批准`、`实施中`、`已完成`。需求、设计、测试和证据使用可追踪编号：`REQ-*` → `DES-*` → `UT/IT/CT/E2E-*` → 自动化测试方法 → 证据文件。

## TDD 是默认开发方式

通过门禁 B 后，涉及新增行为、缺陷修复或可观察行为变化时，必须遵循：

1. 从已批准的测试用例设计中选择一个最小行为，不得实施未批准用例。
2. 每次只选择一个最小行为。
3. 先新增测试并运行，确认它因目标行为缺失而正确失败（RED）。
4. 只实现使该测试通过的最小生产代码（GREEN）。
5. 依次运行聚焦测试、`unit` 测试和受影响的集成测试。
6. 只有相关测试绿色后才能重构；重构不得增加新行为。
7. 最终报告必须包含真实命令、RED 原因、GREEN 结果、回归结果和剩余风险。

不得删除、跳过或弱化关键测试来获得绿色结果。测试第一次就是绿色时，先确认行为是否已存在或断言是否无效。

## 测试分层

- 纯逻辑、领域规则、服务编排测试使用 JUnit `@Tag("unit")`。
- Spring 上下文、数据库、文件系统、网络边界测试使用 `@Tag("integration")`。
- 外部 LLM、Embedding、数据库和对象存储只在真实边界 mock；领域内部优先使用真实对象。
- 新测试名称应表达业务结果，不绑定私有方法或实现步骤。

## 标准命令

- 聚焦测试：`./scripts/tdd-test.ps1 -Mode focus -Test <测试类或类#方法> -Phase RED`
- 单元回归：`./scripts/tdd-test.ps1 -Mode unit -Phase REGRESSION`
- 集成测试：`./scripts/tdd-test.ps1 -Mode integration -Phase REGRESSION`
- PostgreSQL 集成：增加 `-WithPostgres`
- 全量验证：`./scripts/tdd-test.ps1 -Mode all -Phase REGRESSION`

项目级详细流程、模板和已知基线见 `docs/tdd/TDD工作流.md`、`docs/versions/_template/` 与 `docs/tdd/已知基线.md`。

## 变更边界

- 保留用户已有的未提交修改，不覆盖无关文件。
- 公共 API、数据库迁移、配置格式、权限、安全、数据删除和不可逆操作必须先由用户确认。
- 已批准版本不得静默扩大范围；新增需求必须先更新需求、设计和测试追踪关系并重新审核。
- 当前完整测试存在已登记基线问题。试点期间聚焦测试和单元回归必须全绿，完整回归不得新增失败；不得把已知失败当成新改动通过的证据。
