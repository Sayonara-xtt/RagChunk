# V0.8 工程治理基线

## 版本目标

建立后续生产级演化所需的版本、文档、TDD 和 CI 门禁。本版本不改变线上业务行为。

## 任务状态

| 编号 | 任务 | 状态 |
|---|---|---|
| V08-01 | TDD 规则、工作流、模板和已知基线 | 已完成 |
| V08-02 | unit/integration 分层和统一执行脚本 | 已完成 |
| V08-03 | PR 单元测试门禁和证据上传 | 已完成 |
| V08-04 | 文档入口和分区规则 | 已完成 |
| V08-05 | IDE、构建、运行数据和证据忽略规则 | 已完成 |
| V08-06 | 总体版本台账和版本暂停规则 | 已完成 |

## 验收方式

```powershell
.\scripts\tdd-test.ps1 -Mode unit -Phase REGRESSION
.\scripts\tdd-test.ps1 -Mode integration -Phase REGRESSION
git diff --check
```

## 执行证据

### RED

- 现象：`scripts/tdd-test.ps1` 生成的 transcript 只有 PowerShell 头尾，没有 Maven 测试输出，无法作为审计证据。
- 证据：`docs/tdd/evidence/20260827-172232-REGRESSION-unit-suite.log`。

### GREEN

- 最小修改：测试输出通过 `Tee-Object` 同时写入控制台和证据文件，保留 Maven 退出码判断。
- 命令：`.\scripts\tdd-test.ps1 -Mode unit -Phase GREEN`
- 证据：`docs/tdd/evidence/20260827-172503-GREEN-unit-suite.log`。
- 结果：24 个单元测试全部通过，0 失败、0 错误、0 跳过；证据文件包含测试汇总和 `BUILD SUCCESS`。

### 集成基线

- 命令：`.\scripts\tdd-test.ps1 -Mode integration -Phase REGRESSION`
- 证据：`docs/tdd/evidence/20260827-172538-REGRESSION-integration-suite.log`。
- 结果：3 个测试中 1 个出现已登记的重复 `DocPageRedirectController` Bean 错误，2 个环境型测试跳过；没有发现已知基线之外的新失败。

### 文档与仓库检查

- Markdown 本地链接全部可解析。
- `.idea/`、`.ai/`、`target/`、`data/oss-archive/`、临时 Java 副本和本地证据均已加入忽略规则。
- Maven 项目版本更新为 `0.8.0-SNAPSHOT`。
- 未修改生产业务行为。

### 证据持久化修正

- RED：旧证据位于 `target/tdd-evidence/`，执行 Maven `clean` 后探针被删除。
- RED 证据：`docs/tdd/evidence/20260827-RED-evidence-clean-survival.log`。
- 最小修改：证据根目录迁移为 `docs/tdd/evidence/`，并同步测试脚本、CI 上传路径和工作流文档。
- GREEN：`.\scripts\tdd-test.ps1 -Mode unit -Phase GREEN`，24 个单元测试全部通过。
- GREEN 测试证据：`docs/tdd/evidence/20260827-174848-GREEN-unit-suite.log`。
- 生存验证：测试日志在 Maven `clean` 前后均存在且 SHA-256 不变。
- GREEN 生存证据：`docs/tdd/evidence/20260827-GREEN-evidence-clean-survival.log`。

## 已知限制

- PostgreSQL/pgvector 的真实集成验证仍受 Docker/Testcontainers 环境约束。
- 当前 Spring 上下文和旧 E2E 脚本问题继续按 `docs/tdd/已知基线.md` 管理。
- 当前 Windows 本机环境在 `clean` 后首次生产/测试源码编译时曾分别出现一次依赖 JAR 读取型编译器错误；不改代码重跑后完成编译并全绿，后续需继续观察 CI 是否复现。
- 问答运行追踪和质量评测属于 V0.9，不提前进入本版本。

## 版本结论

V0.8 已完成。按总体执行规则，本版本结束后停止，等待是否进入 V0.9 的人工指令。
